{
  "name": "shade-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shade — private payments on Stellar via stealth addresses (DKSAP)",
  "scripts": {
    "test": "npm run test --workspaces --if-present && cd contracts && cargo test --quiet",
    "build": "npm run build --workspaces --if-present",
    "demo": "bash demo.sh",
    "stress-test": "tsx packages/cli/test/stress-test.ts",
    "relayer:dev": "cd packages/relayer && npm run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.5.0"
  },
  "license": "Apache-2.0"
}
